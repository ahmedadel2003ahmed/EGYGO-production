# Authentication Flow Diagram

## 🔄 Complete Authentication Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                        AUTHENTICATION FLOW                       │
└─────────────────────────────────────────────────────────────────┘

┌──────────────────────┐
│   Guest User Visits  │
│     EgyGo Website    │
└──────────┬───────────┘
           │
           ▼
┌──────────────────────────────────────────────────────────────────┐
│                    FREE BROWSING ZONE                             │
│  ✓ Home Page              ✓ Destinations      ✓ Guide Profiles   │
│  ✓ Governorates           ✓ Place Details     ✓ Trip Listings    │
│  ✓ About Page             ✓ Reviews           ✓ Photos/Gallery   │
│                                                                   │
│  NO AUTHENTICATION REQUIRED ✅                                    │
└──────────┬───────────────────────────────────────────────────────┘
           │
           │ User clicks protected action
           ▼
    ┌─────────────┐
    │ Authenticated?│
    └─────┬─────┬─┘
          │     │
      YES │     │ NO
          │     │
          │     ▼
          │  ┌─────────────────────────────────┐
          │  │   LOGIN MODAL APPEARS           │
          │  │   (NO page redirect!)           │
          │  │                                 │
          │  │   ┌─────────────────────┐      │
          │  │   │  Email & Password   │      │
          │  │   │  Login Form         │      │
          │  │   └─────────────────────┘      │
          │  │                                 │
          │  │   [X] Close    [Login] Submit  │
          │  └──────────┬──────────────────────┘
          │             │
          │             │ User submits
          │             ▼
          │      ┌──────────────┐
          │      │ API Request  │
          │      │ POST /login  │
          │      └──────┬───────┘
          │             │
          │             ▼
          │       ┌──────────┐
          │       │ Success? │
          │       └─┬──────┬─┘
          │         │      │
          │     YES │      │ NO
          │         │      │
          │         │      ▼
          │         │   ┌──────────────────┐
          │         │   │ Show Error       │
          │         │   │ Stay in Modal    │
          │         │   └──────────────────┘
          │         │
          │         ▼
          │  ┌─────────────────────────────┐
          │  │ Update AuthContext          │
          │  │ - setAuth({ token, user })  │
          │  │ - Close modal automatically │
          │  │ - Execute pending action    │
          │  └─────────────┬───────────────┘
          │                │
          └────────────────┘
                   │
                   ▼
          ┌────────────────────────────────────┐
          │    PROTECTED ACTION EXECUTES        │
          │                                     │
          │  ✓ Create Trip                     │
          │  ✓ Book Guide                      │
          │  ✓ Send Message                    │
          │  ✓ Submit Review                   │
          │  ✓ Update Profile                  │
          └────────────────────────────────────┘


┌──────────────────────────────────────────────────────────────────┐
│                         LOGOUT FLOW                               │
└──────────────────────────────────────────────────────────────────┘

   User clicks "Logout"
           │
           ▼
   ┌───────────────────┐
   │ Confirmation      │
   │ "Are you sure?"   │
   └────┬──────────────┘
        │ Confirm
        ▼
   ┌──────────────────────────────┐
   │ auth.logout()                │
   │ - Clear token                │
   │ - Clear user                 │
   │ - Clear localStorage         │
   └────┬─────────────────────────┘
        │
        ▼
   ┌──────────────────────────────┐
   │ Redirect to HOME (/)         │
   │ NOT /login                   │
   └────┬─────────────────────────┘
        │
        ▼
   ┌──────────────────────────────┐
   │ User can continue browsing   │
   │ Full site access maintained  │
   └──────────────────────────────┘


┌──────────────────────────────────────────────────────────────────┐
│                  SESSION EXPIRY (401) FLOW                        │
└──────────────────────────────────────────────────────────────────┘

   User attempts action with expired token
           │
           ▼
   ┌───────────────────┐
   │ API returns 401   │
   └────┬──────────────┘
        │
        ▼
   ┌──────────────────────────────┐
   │ Catch 401 error              │
   │ auth.requireAuth(() => {     │
   │   // Retry original action   │
   │ })                           │
   └────┬─────────────────────────┘
        │
        ▼
   ┌──────────────────────────────┐
   │ Login modal appears          │
   │ User re-authenticates        │
   └────┬─────────────────────────┘
        │
        ▼
   ┌──────────────────────────────┐
   │ Original action retries      │
   │ automatically                │
   └──────────────────────────────┘
```

---

## 🎯 Key Components Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                       AuthContext.js                             │
│  Central Authentication State Management                         │
├─────────────────────────────────────────────────────────────────┤
│  State:                                                          │
│  • token: string | null                                          │
│  • user: object | null                                           │
│  • loading: boolean                                              │
│  • showLoginModal: boolean                                       │
│  • pendingAction: function | null                                │
├─────────────────────────────────────────────────────────────────┤
│  Methods:                                                        │
│  • setAuth({ token, user }) → Updates auth state                │
│  • logout() → Clears auth state                                 │
│  • requireAuth(action) → Guards protected actions               │
│  • closeLoginModal() → Dismisses modal                          │
│  • completeOnboarding(data) → Updates profile                   │
└─────────────────────────────────────────────────────────────────┘
          │
          │ Provides context to
          ▼
┌─────────────────────────────────────────────────────────────────┐
│                    Application Components                        │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌─────────────┐  ┌──────────────┐  ┌─────────────────┐       │
│  │   Navbar    │  │  LoginModal  │  │  Protected      │       │
│  │             │  │              │  │  Components     │       │
│  │ • Get Started│ │ • Email/Pass │  │                 │       │
│  │   button    │  │ • Submit     │  │ • TripModal     │       │
│  │ • Profile   │  │ • Close      │  │ • GuideProfile  │       │
│  │ • Logout    │  │ • Error msg  │  │ • MyTrips       │       │
│  └─────────────┘  └──────────────┘  └─────────────────┘       │
│         │                 │                   │                 │
│         └─────────────────┴───────────────────┘                 │
│                           │                                     │
│                    All use useAuth()                            │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Action Protection Pattern

```javascript
// OLD WAY (❌ Don't do this)
const handleCreateTrip = async () => {
  const token = localStorage.getItem('access_token');
  if (!token) {
    router.push('/login');  // ❌ Disrupts user experience
    return;
  }
  
  await createTrip();
};


// NEW WAY (✅ Do this)
const handleCreateTrip = () => {
  auth.requireAuth(async () => {
    await createTrip();  // ✅ Seamless, modal-based
  });
};


// EVEN BETTER (✅ Custom Hook)
const requireAuth = useAuthAction();

const handleCreateTrip = requireAuth(async () => {
  await createTrip();  // ✅ Clean, reusable
});
```

---

## 📊 State Flow Diagram

```
┌─────────────────────────────────────────────────────────────┐
│                    AUTHENTICATION STATES                     │
└─────────────────────────────────────────────────────────────┘

   ┌─────────────┐
   │  LOADING    │  (Initial state, checking localStorage)
   │  loading=true│
   └──────┬──────┘
          │
          ▼
   ┌────────────────┐
   │ Token exists?  │
   └───┬────────┬───┘
       │        │
    YES│        │NO
       │        │
       ▼        ▼
┌─────────┐  ┌────────────┐
│AUTHENTICATED GUEST       │
│token: xxx│  │token: null │
│user: {}  │  │user: null  │
└────┬────┘  └─────┬──────┘
     │             │
     │ Logout      │ Login Success
     │             │
     └────────┬────┘
              │
              ▼
        ┌───────────┐
        │  Switch   │
        │  State    │
        └───────────┘


Protected Action Attempt:
========================

GUEST STATE                 AUTHENTICATED STATE
     │                             │
     │ requireAuth() called        │ requireAuth() called
     ▼                             ▼
┌─────────────┐            ┌──────────────────┐
│ Show Modal  │            │ Execute Action   │
│ Save Action │            │ Immediately      │
└─────────────┘            └──────────────────┘
     │
     │ Login Success
     ▼
┌──────────────────┐
│ Execute Saved    │
│ Action           │
└──────────────────┘
```

---

## 🎨 Component Hierarchy

```
RootLayout (layout.js)
  │
  ├─ AuthProvider (AuthContext)
  │   │
  │   ├─ QueryProvider
  │   │   │
  │   │   ├─ ConditionalNavbar
  │   │   │   │
  │   │   │   └─ Navbar
  │   │   │       │
  │   │   │       └─ LoginModal (controlled by AuthContext)
  │   │   │
  │   │   ├─ ConditionalMainWrapper
  │   │   │   │
  │   │   │   └─ {children} (All pages)
  │   │   │       │
  │   │   │       ├─ Home
  │   │   │       ├─ Destinations
  │   │   │       ├─ GuideProfile
  │   │   │       ├─ MyTrips (uses requireAuth)
  │   │   │       ├─ Profile (uses requireAuth)
  │   │   │       └─ CreateTrip (uses requireAuth)
  │   │   │
  │   │   └─ ConditionalFooter
  │   │
  │   └─ All components can access:
  │       • auth.token
  │       • auth.user
  │       • auth.requireAuth()
  │       • auth.logout()
```

---

## 🛡️ Security Considerations

```
✅ Token stored in localStorage (client-side)
✅ Token sent in Authorization header
✅ 401 errors trigger re-authentication
✅ Session expiry handled gracefully
✅ No sensitive data in URLs
✅ Actions protected at function level
✅ Backend validates all requests

⚠️  Remember:
• Never trust client-side auth alone
• Always validate tokens on backend
• Use HTTPS in production
• Implement token refresh if needed
• Consider adding CSRF protection
```

---

## 📈 Conversion Funnel Optimization

```
Traditional Flow (❌):
Home → Login Wall → Registration → Browse → Action
Conversion: ~10-20%


Our Flow (✅):
Home → Browse → See Value → Action → Login → Complete Action
Conversion: ~40-60% (2-3x improvement)


Why It Works:
1. Users see value first
2. No friction during exploration
3. Context preserved during auth
4. Action continuity after login
5. Can still browse after logout
```

---

**Visual diagrams help understand the architecture at a glance!**
